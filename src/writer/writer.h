#ifndef Writer_h
#define Writer_h

/* Responsible for saving the neural net as a text file.
 */

class Network;  //  ../network/network.h
class Neuron;   //  ../neuron/neuron.h

class Writer
{
    public:
        Writer();
        //void dump_network_data(Network& network);
        //void process_network_layer(std::vector<Neuron>& layer);
};

#endif
